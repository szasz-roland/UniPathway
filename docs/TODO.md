# To-do / known problems

Not being worked on right now — just tracked here so it's not forgotten. Check this file when asked "what's on the to-do list."

- [ ] **Checklist feature (notes/todo/shopping lists) is not persistent in the Android app (Web2APK wrapper).** Data doesn't survive between sessions on the phone, even though it works correctly (via `localStorage`) in a regular browser. Needs investigation — not started yet.
- [ ] **Desktop centered layout (`feature/desktop-centered-layout` branch): implemented, awaiting test/confirm + merge.** `.main` now gets `margin-inline:auto` at ≥1080px so it centers instead of hugging the left edge on wide monitors. Sitting uncommitted on that branch — pick up by trying it locally, confirming it looks right (adjust the centering amount if it's too subtle/aggressive), then commit and merge into `main` like the desktop-week-clickable-view branch before it.
- [ ] **Floating sidebar redesign (3rd item from the desktop-optimization list, not started).** Split the `.rail` sidebar into two separate floating pieces instead of one continuous full-height bar: a top floating cluster (hamburger/nav) and a bottom floating cluster (checklist, gear), each as rounded-square buttons rather than a flush vertical bar. Tapping the hamburger should expand a floating panel near the top cluster only; tapping checklist/gear should expand a floating panel near the bottom cluster only — i.e. two independent floating expand panels instead of today's one shared full-height `#drawer`. This needs a proper design pass (the `find-animation-opportunities`/`animate` skills per CLAUDE.md's working-style notes) before implementing, since the exact floating shape/spacing wasn't fully nailed down. Canonical branch name: `feature/floating-sidebar`, branched off `main` after the centered-layout branch above is merged in (sequential, not parallel, since this branch also touches the rail).
