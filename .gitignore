# --- Secrets & credentials (never commit these) ---
.env
.env.*
!.env.example
*.pem
*.key
*.keystore
*.jks
*.p12
secrets.json
service-account*.json

# --- Android / Web2APK signing & build artifacts ---
*.apk
*.aab
*.idsig
keystore.properties
local.properties
android/build/
android/.gradle/
android/app/build/

# --- Node / future tooling (Phase 2+) ---
node_modules/
dist/
.vercel/
npm-debug.log*
yarn-error.log*

# --- Local Claude Code skill packages (third-party, unclear license — keep out of the public repo) ---
.claude/skills/

# --- Raw source exports for course info/requirements (PDFs from unics.szte.hu) — kept locally only; the app reads processed data instead ---
course_info_req/
course_info/
course_req/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
.vscode/*
!.vscode/extensions.json
